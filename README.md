---- Personal Expense Tracker ----

The data is stored within a JSON file with each item (expense) is identified by a unique number which increases by 1 with each new expense added. 
The data passed to be stored is the date of the expense as a string, formatted as YYYY-MM-DD; the expense amount as a float; and the category as a string.
