---- Personal Expense Tracker ----

The data is stored within JSON file with each item (expense) is identified by a unique number which increases by 1 with each new expense added. 
The data passed and stored is the date of the expense in a string, formatted as YYYY-MM-DD; the expense amount as a float; and the category which is also stored as a string.
